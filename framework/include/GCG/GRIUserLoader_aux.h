  if (object_name == "A1") {
    p = new SIMAnalysisThread();
    return p;
  }

  if (object_name == "SIMDAQ1") {
    p = new SIMDAQThread();
    return p;
  }

  if (object_name == "SIMDAQ2") {
    p = new SIMDAQThread();
    return p;
  }

